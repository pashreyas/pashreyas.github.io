import React, { useEffect, useRef } from 'react';
import styled from 'styled-components';
import { srConfig } from '@config';
import sr from '@utils/sr';
import { usePrefersReducedMotion } from '@hooks';

const StyledAboutSection = styled.section`
  max-width: 900px;

  .inner {
    display: grid;
    grid-template-columns: 8fr 2fr;
    grid-gap: 50px;

    @media (max-width: 768px) {
      display: block;
    }
  }
`;
const StyledText = styled.div`
  ul.skills-list {
    display: grid;
    grid-template-columns: repeat(2, minmax(200px, 300px));
    grid-gap: 0 10px;
    padding: 0;
    margin: 20px 0 0 0;
    overflow: hidden;
    list-style: none;

    li {
      position: relative;
      margin-bottom: 10px;
      padding-left: 20px;
      font-family: var(--font-mono);
      font-size: var(--fz-xs);

      &:before {
        content: '▹';
        position: absolute;
        left: 0;
        color: var(--green);
        font-size: var(--fz-sm);
        line-height: 12px;
      }
    }
  }
`;

const About = () => {
  const revealContainer = useRef(null);
  const prefersReducedMotion = usePrefersReducedMotion();

  useEffect(() => {
    if (prefersReducedMotion) {
      return;
    }

    sr.reveal(revealContainer.current, srConfig());
  }, []);

  const skills = ['MATLAB', 'Excel', 'R Statistical Analysis', 'WordPress'];
  const skills_es = ['Davinci', 'Resolve Studio', 'Final Cut Pro'];
  const skills_m = ['Singing (Indian classical)', 'Drums (percussion)', 'Keyboard', 'Piano'];

  return (
    <StyledAboutSection id="about" ref={revealContainer}>
      <h2 className="numbered-heading">About Me</h2>

      <div className="inner">
        <StyledText>
          <div>
            <p>
              Hello! My name is Shreyas and I enjoy learning about the ways I can
              connect my other passions, such as global politics, film production, poetry,
              and art to the development and application of holistic healthcare.
            </p>

            <p>Here are a few key Tools and Skills:</p>
          </div>

          <ul className="skills-list">
            {skills && skills.map((skill, i) => <li key={i}>{skill}</li>)}
          </ul>
          <div>
            <p></p>
            <p>Editing Software:</p>
          </div>
          <ul className="skills-list">
            {skills_es && skills_es.map((skill, i) => <li key={i}>{skill}</li>)}
          </ul>
          <div>
            <p></p>
            <p>Music:</p>
          </div>
          <ul className="skills-list">
            {skills_m && skills_m.map((skill, i) => <li key={i}>{skill}</li>)}
          </ul>
        </StyledText>
      </div>
    </StyledAboutSection>
  );
};

export default About;
